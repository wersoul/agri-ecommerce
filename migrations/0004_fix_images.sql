-- =========================================
-- KNK Part: Fix product images to use ASCII-safe filenames
-- ⚠️  Production - ล้างข้อมูลตัวอย่างแล้ว — ไม่มี UPDATE ในไฟล์นี้
-- =========================================
-- (ไม่มีข้อมูลตัวอย่างในไฟล์นี้)
